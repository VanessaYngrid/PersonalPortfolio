/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    
    //if I want to use a image from internet
    /*images: {
        remotePatterns: [
            {
            protocol: 'https',
            hostname: 'www.themealdb.com',
            port: '',
            pathname: '/images/media/meals/**',
            },
        ],
        },*/
};

export default nextConfig;
